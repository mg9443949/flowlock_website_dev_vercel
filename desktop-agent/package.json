{
  "name": "flowlock-agent",
  "version": "1.0.0",
  "description": "Desktop agent for FlowLock to block distracting apps.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . FlowLockAgent --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}
